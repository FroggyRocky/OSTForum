export interface INetwork {
    id:number,
    header:string
    description:string
    cover:string
    link:string
    keyIds:number[]
    createdAt:string
}
export interface ICreatedNetwork {
    header:string
    description:string
    cover:string
    link:string
    keyIds:number[]
}